// MDD test skeleton — 02-jsonforms-studio
// Component tests for the center canvas: live JsonForms preview plus drop zones
// and selection overlay.

describe("jsonforms-studio / Canvas", () => {
  it.skip("should render a live JsonForms preview of the current schema and uiSchema", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should display an empty-state hint when the uiSchema tree has no elements", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should accept a drop from the palette and insert the node at the drop position", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should insert as a sibling above/below based on the vertical midpoint when dropping onto a Control", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should select a node when clicked and highlight it in the overlay", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should badge Controls whose scope does not resolve", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should support reordering siblings within a layout via drag", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should disable drop zones and selection clicks in readonly mode", () => {
    throw new Error("Not implemented — MDD skeleton");
  });

  it.skip("should respond to Cmd/Ctrl+Z with undo and Cmd/Ctrl+Shift+Z with redo", () => {
    throw new Error("Not implemented — MDD skeleton");
  });
});

# Application
VITE_APP_NAME=Single Digital Gateway

# API
VITE_API_URI=http://localhost:5173/api

# Auth (direct API URL for browser redirects — bypasses Vite proxy)
VITE_AUTH_URL=http://localhost:4000

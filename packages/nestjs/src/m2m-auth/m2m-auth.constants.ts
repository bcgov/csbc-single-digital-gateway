/** Resolved {@link M2mAuthModuleOptions}. */
export const M2M_AUTH_OPTIONS = Symbol('M2M_AUTH_OPTIONS');

/** The {@link M2mTokenVerifier} port — `verify(token) -> M2mPrincipal`, throws on invalid. */
export const M2M_TOKEN_VERIFIER = Symbol('M2M_TOKEN_VERIFIER');
